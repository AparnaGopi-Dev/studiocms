<div class="mb-6">

    <h1 class="text-2xl font-light text-gray-800">
        {{ $title }}
    </h1>

    @if(isset($subtitle))
        <p class="text-gray-500 mt-1">
            {{ $subtitle }}
        </p>
    @endif

</div>